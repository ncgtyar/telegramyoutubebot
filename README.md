# telegramyoutubebot
Telegram bot made with Python. Entries are pulled from YouTube: User's message is used as the search input and the top result is processed into an .mp3 file and temporarily cached in the host machine, deleted after the file is sent in the chat.
